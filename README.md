# 数字安灯事件编排

本项目把设备原始告警归并为可处置的生产事件，并在安全许可约束下推进处置步骤。原始信号不会因抑制通知、事件关联或结案而删除。

`domain_contract.json` 给出事件、处置剧本和控制权状态。设备依赖关系有生效版本，自动回调必须携带执行实例与步骤标识。

运行基础检查：

```bash
python -m unittest discover -s tests -v
```

